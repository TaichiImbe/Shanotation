<!DOCTYPE html>
<html lang="ja">
<html>

<head>
  <% include common/_head %>
  <!-- とりあえずコネクション周りは残しておく -->
  <script src='/socket.io/socket.io.js'></script>
  <script>
    // var socket = io.connect('http://192.168.178.107:4000');
    // var socket = io.connect('http://192.168.2.110:4000');
    var socket = io.connect('ec2-18-221-83-167.us-east-2.compute.amazonaws.com:4000');
    // var socket = io.connect('192.168.11.64:4000');

    socket.on('connect', function (msg) {
      console.log('connect');
    });
    function send(obj) {
      // socket.emit('object',obj);
    }

    socket.on('teacher', function (data,pageNum) {
      console.log(data);
      makeLine(data,pageNum);
      // Canvas.add(data);
    });
  </script>

</head>

<body>
  <% include common/_function %>
  <% include common/_fabric %>
</body>

</html>
<!DOCTYPE html>

</html>